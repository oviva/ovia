<%= simple_form_for [:admin, @user] do |f| %>
	<%= f.error_messages %>
	<%= f.input :email %>
	<%= f.input :password %>
	<%= f.input :admin, :label => false, :inline_label =>"Is an admin?" %>
	<%= f.button :submit, :class => 'btn btn-primary' %>
<% end %>