<h1><%= @size.name %></h1>
<%= link_to "Edit Size" , edit_admin_size_path(@size), 
	:class => 'btn btn-primary' %>

<%= link_to "Delete Size" , admin_size_path(@size), 
	:class => 'btn', :method => :delete, 
	:confirm => "Are you sure you want to delete this product?" %>