<%= simple_form_for [:admin, @page] do |f| %>
	<%= f.error_messages %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :content %>
	<%= f.text_area :content %><br/>
	<%= f.submit :class => 'btn' %>
<% end %>