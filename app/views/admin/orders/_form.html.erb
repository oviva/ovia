<%= simple_form_for [:admin, @order] do |f| %>
	<%= f.error_messages %>
	<%= f.input :order_no %>
	<%= f.input :total_amount %>
	<%= f.button :submit, :class => 'btn btn-large btn-primary' %>
<% end %>